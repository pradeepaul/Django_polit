# Install Django

To create virtual environemt
# pip install pipenv

To start the virtual environment/
# pipenv shell 

To install Django in virtual environment
# pipenv install django

To create project in django virtudal environemnt
# django-admin startproject {{project_name}}

To create a application inside the django project
# python manage.py startapp {{app_name}}
